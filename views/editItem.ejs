<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Student List App</title>
    </head>
<body>
    <nav class="navbar navbar-dark bg-primary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">MyCollection App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addItem">Add New Item</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Edit Item</h2></div>
        <br>
        <div class="text-center">
          <% if (item) { %>
            <form action="/editItem/<%= item.id %>" method="POST" enctype="multipart/form-data">
              <label for="name">Item Name:</label><br>
              <input class="form-control" aria-label="Name" aria-describedby="basic-addon1" type="text" id="name" name="name" value= "<%= item.name %>" required><br><br>
  
              <label for="variant">Variant:</label><br>
              <input class="form-control" aria-label="Name" aria-describedby="basic-addon1" type="text" id="variant" name="variant" value= "<%= item.variant %>" required><br><br>
  
              <label for="history">Item History:</label><br>
              <div class="input-group">
                <input class="form-control" aria-label="With textarea" type="text" id="history" name="history" value= "<%= item.history %>" ><br><br>
              </div>
  
              <label for="image">Current Image:</label><br>
              <input type="text" name="currentImage" value= "<%= item.image %>" disabled><br>
              <img src="/images/<%= item.image %>" width="20%"><br><br>
              
              New Image:<br>
              <div class="input-group mb-3">
                <input type="file" class="form-control" id="inputGroupFile02" type="file" id="image" name="image" accept="image/*" value="<%= item.image %>">
              </div>

              <button class="btn btn-primary" type="submit">Save Changes</button>
            </form>
          <% } else { %>
              <p>No item found.</p>
          <% } %>
          <a href ="/item/<%= item.id %>">Back</a>
        </div>
    </div>
</body>
</html>
